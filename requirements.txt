# TensorFlow implementation (FAEclust) — core dependencies.
# Install: pip install -r requirements.txt   (or: pip install -e .)
# Optuna hyperparameter tuning is OPTIONAL: pip install optuna

tensorflow>=2.16
keras>=3.0
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
PyWavelets>=1.5
networkx>=3.0
joblib>=1.3
numba>=0.59          # JIT for the elastic SRVF DP (~100x); falls back to pure-NumPy if absent
matplotlib>=3.7
# optional, only for FAEclust.tuning:
# optuna>=3.4
# optional, only for test_tf.py (UCR "Plane"):
# aeon>=0.10
